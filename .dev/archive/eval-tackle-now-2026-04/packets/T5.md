# Executor packet T5 — Audit remediation (2026-04-20)

Follow [executor-subtask-execution](file:///C:/Users/Ale/.cursor/skills/executor-subtask-execution/SKILL.md) for execution discipline.

**Audit source:** `.dev/audits/2026-04-20-eval-tackle-now-2026-04.md` · **Verdict:** `fail` · **Plan baseline:** `eval-tackle-now-2026-04` v1.1.

---

## Section 1 — Task statement (verbatim) + amendment scope

Hermes eval needs four coordinated closures from the Part A residual list: **(1)** stop silently relying on index order for multi-record goldens when no `match_key` is set (A-01), **(2)** define how non-mocked / live-LLM and nightly-style runs should interpret pass/fail under output variance (policy and, where agreed, machine-readable hooks), **(3)** prove the eval harness can score a job that was completed via `resume_pipeline` (A-04 integration gap), and **(4)** record the product stance and technical limits for “extra” / hallucinated fields when goldens are empty or schema-agnostic (F-14 / A-03) so future work is gated.

**Non-goals**

- Generic record matching algorithms (Hungarian, schema introspection) beyond existing `match_key` pairing — see `.dev/EVAL.md` “Explore generic matching (optional)”.
- Refactoring `scorer.py` for size alone (explicitly deferred in EVAL.md).
- Golden object-vs-array form (F-07) unless a separate plan is opened.
- Implementing F-14 “hallucination detection” as a product feature without explicit product approval.

**Amendment scope (T5 only)**

Post-audit remediation for **`.dev/audits/2026-04-20-eval-tackle-now-2026-04.md`**. Close **documentation and optional test** gaps; do **not** change eval product behavior unless F3 is implemented as a regression test only.

**Amendment non-goals**

- Re-opening T1–T4 design decisions.
- New CLI flags, manifest keys, or T2 policy changes.

---

## Section 2 — Shared contracts (verbatim; amendment overlays)

| Topic | Binding |
|--------|---------|
| **Types / interfaces** | **Existing:** `EvalManifest.match_key` (`hermes/eval/manifest.py`), `_field_diffs_for_records(..., match_key)` (`hermes/eval/scorer.py`), `run_eval_suite(..., results_mode, job_id, ...)` (`hermes/eval/runner.py`), `run_pipeline(..., force_new_job=...)` / `resume_pipeline(job_id)` (`hermes/extraction/pipeline.py`). **T1:** If multi-record validation is tightened, failure is surfaced at manifest load / golden resolution with `ValueError` messages stable enough for tests to assert substrings (no error codes in HTTP sense — library / CLI). **T2:** Any **new** user-visible eval knobs (env vars, CLI flags, manifest keys, aggregation result shapes) must name owning subtask, land on a typed parse path (dataclass / Pydantic / Typer option), and have a construction or round-trip test — or be explicitly marked *deferred to a follow-up subtask ID* with no partial `getattr` defaults. **T3:** No new public API required; uses `ResultsMode.FROM_JOB` and existing DB helpers. **T4:** Prose-only unless product approves a new manifest flag; if a flag is added later, it becomes a new plan version. |
| **Error envelope** | Manifest and runner continue to raise `ValueError` for author-time mistakes; eval CLI maps `ValueError` to exit code 1 (`hermes/cli.py` `eval` command). No new HTTP or RPC contracts. |
| **Naming** | Prefer existing terms: `match_key`, `ResultsMode`, `FROM_JOB`, `force_new_job`. New symbols must be grep-unique under `hermes/eval/`. |
| **Logging** | Use module loggers (`hermes.eval.manifest`, `hermes.eval.scorer`, `hermes.eval.runner`); no new structured fields required unless T2 introduces aggregation metrics (then document field names in T2 packet §2). |
| **Tests** | `pytest`; tests colocated under `tests/` with `test_*.py`; match existing eval tests (`tests/test_eval_*.py`, `tests/test_pipeline_integration.py`). |
| **CLI surface** | **Frozen unless T2 explicitly extends:** `hermes eval` — `--fixture-dir`, `--manifest`, `--from-results`, `--from-jsonl`, `--update-goldens`, `--yes`, `--output`, `--model`, `--verbose` (`hermes/cli.py`). T3 must not require new flags. |

**T5 overlay:** No new user-visible typed surfaces. F3 (optional) adds or extends **tests only** that assert existing `run_pipeline(..., force_new_job=True)` at the pipeline eval call site — acceptable under §2 Tests row.

**Typed-surface binding note:** T5 does not introduce config keys or CLI strings.

---

## Section 4 — Subtask T5

| Field | Content |
|--------|---------|
| **ID** | T5 |
| **Scope** | Remediate audit **F1** (major), **F2** (minor), and optionally **F3** (observation), per `.dev/audits/2026-04-20-eval-tackle-now-2026-04.md` §3–§7. |
| **Files to touch** | **F1 (required):** `.dev/EVAL.md` — row 6 / A-04 “Tackle now” table: replace present-tense claim that there is **no** A-04 eval integration test; cite `tests/test_eval_resume_integration.py` and accurately describe coverage (`resume_pipeline` → `run_eval_suite` / `ResultsMode.FROM_JOB`). Adjust **Primary files / refs** column so it does not imply only `test_pipeline_integration.py`. **F2 (required):** `README.md` — fix awkward inline-code / bold markup around the T2 decision-log path in “How we measure quality” (audit: `` `**.path**` `` style); use clean markdown link or monospace path. **F3 (optional):** `tests/test_eval_runner.py` (or smallest appropriate test module) — assert `run_eval_suite` pipeline path passes `force_new_job=True` into `run_pipeline` (patch/mock `run_pipeline` and inspect kwargs **or** equivalent static assertion per repo patterns). |
| **Contract bindings** | Plan §2 + T5 overlay; no new CLI or manifest schema. |
| **Inputs** | Audit report (above path); existing `hermes/eval/runner.py` `run_pipeline` call site (~408–417). |
| **Outputs** | F1 + F2 merged; changelog line under appropriate heading noting audit remediation if project convention requires it; F3 either landed with passing pytest or explicitly deferred in executor notes with rationale. |
| **Kill criteria** | **HALT** if `.dev/EVAL.md` still contradicts landed code (integration test exists) after edit — re-read table in “Tackle now”. **HALT** if F3 implementation would require changing `runner.py` behavior (it must not; test only observes current contract). |
| **Log tier** | `trivial` for F1+F2 only; `standard` if F3 test is added (brief changelog: what + why). |
| **Risks & mitigations** | F3 over-mocking: keep test minimal — one call-site assertion sufficient. |

---

## Audit mapping (executor checklist)

| Audit ID | Severity | Action |
|----------|----------|--------|
| **F1** | major | Update `.dev/EVAL.md` A-04 row to reflect `tests/test_eval_resume_integration.py`; remove “no integration test” false claim. |
| **F2** | minor | README readability for decision-log reference. |
| **F3** | observation | Optional regression test for `force_new_job=True` on eval pipeline path. |

---

## Filtered load-bearing assumptions

- `tests/test_eval_resume_integration.py` remains the canonical A-04 integration test unless renamed — update EVAL references if rename occurs.

---

## Filtered hidden couplings

- **Plan §8 / §9:** After F1, update **`plan.md` v1.2** narrative (completion summary and/or Notes for auditor item 3) so “stale EVAL row” is marked **remediated by T5**, not still open.

---

## Resolved inputs

- **to be supplied at execution time:** N/A — audit text is committed at `.dev/audits/2026-04-20-eval-tackle-now-2026-04.md`.

---

## Post-execution (executor)

- Append a one-line **audit status** note to `.dev/audits/2026-04-20-eval-tackle-now-2026-04.md` or add a short “Remediation” subsection pointing to merge commit — **only if** repo convention expects it; otherwise changelog + plan bump suffice.
