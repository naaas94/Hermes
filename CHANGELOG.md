# Changelog

## [schema 2.0] — 2026-04-16

- T5: README **Benchmarks & memory** (claims, `hermes bench` usage, reference-hardware table from committed `benchmarks/<date>_<sha>.json`, methodology); Part B roadmap items in `.dev/evaluation-and-health-metrics-roadmap.md` marked done.
- **CI bench artifact T6** (`.github/workflows/ci.yml`): on **push to `main` only**, after tests, run **`hermes bench --mock-llm --output benchmarks`** (writes **`benchmarks/<YYYYMMDD>_<short-sha>.json`**), **`continue-on-error`** with a **5-minute** step timeout, and upload artifact name **`bench-summary-<full-sha>`** (`actions/upload-artifact`). No threshold gate; artifacts are informational (runner RSS/timing vary). Skipped bench steps (non-main / failed job before bench) do not upload.
- **Benchmark harness T4** (`hermes/bench/runner.py`, `hermes/bench/workloads.py`, `hermes/cli.py`, `benchmarks/.gitkeep`, `tests/test_bench_runner.py`): add **`hermes bench`** with **`BenchWorkload` / `BenchResult` / `BenchSummary`**, **`run_bench`** (mockable pipeline, optional dual **console** vs **JSON** NDJSON comparison when **structlog** is installed, **`dual_sink_overhead_pct`** and **`bench.dualsink.regression`** warning when overhead exceeds 10% on **`pdf_text_small`**), in-process RSS peak from **`rss.sample`** plus **`resource.getrusage`** where available, and **`benchmarks/<YYYYMMDD>_<short-sha>.json`** (+ optional **`.csv`**). Standard workloads use committed **`tests/fixtures/eval`** fixtures and optional repo-root stress PDF with **`--include-large`**. **Decision log:** `.dev/part_b/decisions/T4.md`.
- **RSS sampling T3** (`hermes/obs/sampling.py`, `hermes/extraction/pipeline.py`, `tests/test_obs_sampling.py`): add **`sample_rss`**, **`stage_timer`** ( **`stage.start`** / **`stage.end`** plus **`rss.sample`** at boundaries; optional **`rss_sampling_interval_s`** periodic thread for **`normalization`** only), optional **`psutil`** with one-time **`rss.sample.unavailable`** warning, and wire stages through **`bind_job`** + timers at preflight, normalization, chunking, and extraction. Failures in emit paths are suppressed per the observability error envelope; stage errors are recorded on **`stage.end`** **`detail`** then re-raised.
- **Observability logging T2** (`hermes/obs/logging.py`, `hermes/config.py`, `hermes/cli.py`, `tests/test_obs_logging.py`): add **`ObservabilityConfig`** (`log_format`, `log_dir`, `log_ndjson`) and **`configure_logging` / `get_logger` / `bind_job`**. Default console format matches the previous **`basicConfig`**; optional NDJSON append under **`hermes-<YYYYMMDD>.ndjson`**. **`log_format="json"`** requires structlog via **`[obs]`** or startup raises **`HermesObsExtraRequired`**. Invalid **`log_format`** falls back to console with a warning. **Decision log:** `.dev/part_b/decisions/T2.md`.
- **Observability schema T1.1** (`hermes/obs/schema.py`, `tests/test_obs_schema.py`): bump **`LogSchemaVersion`** / **`CURRENT_LOG_SCHEMA_VERSION`** to **`2.0`**. Narrow **`StageName`** to **`preflight`**, **`normalization`**, **`chunking`**, **`extraction`** (same strings as `save_pipeline_stage` / `pipeline_stages.stage`). Remove **`repair`** / **`export`** as stage labels; repair attempts are observed via **`llm.call.run_type`** (`extraction` \| `retry` \| `repair`, default **`extraction`**). Add **`HermesObsExtraRequired`** for the v0.2 contract: JSON NDJSON logging requires the **`[obs]`** extra (enforced in **`configure_logging`**, T2). **Decision log:** `.dev/part_b/decisions/T1.md` (v0.2 section).

**Migration (v1.0 NDJSON → v2.0)** (no runtime rewriter; drop or re-emit dev logs as needed):

| Old (v1.0) `stage` | New (v2.0) | Note |
|--------------------|------------|------|
| `"preflight"` | `"preflight"` | Unchanged |
| `"normalize"` | `"normalization"` | Rename |
| `"chunk"` | `"chunking"` | Rename |
| `"extract"` | `"extraction"` | Rename |
| `"repair"` | *(reclassify)* | Not a stage; use `llm.call` with `run_type="repair"` |
| `"export"` | *(drop)* | Not a stage in v2.0 |

## 2026_04_16

- **Observability schema T1** (`hermes/obs/schema.py`, `hermes/obs/__init__.py`, `tests/test_obs_schema.py`, `pyproject.toml`): introduce version **`1.0`** NDJSON event catalog (`EventName`, `StageName`, `LogSchemaVersion`, `CURRENT_LOG_SCHEMA_VERSION`), common **`BaseEvent`** fields, per-event Pydantic models and **`HermesLogEvent`** discriminated union, **`EVENT_FIELD_CATALOG`** for documented field sets, and **`validate_event()`** for non-throwing validation in tests and callers. Pipeline DB stages use different spellings than logging (**e.g.** `normalization` vs `normalize`); **`StageName`** is the canonical observability vocabulary per Part B. Optional **`[obs]`** extra lists **`structlog`** and **`psutil`** for upcoming logging/RSS work. **Decision log:** `.dev/part_b/decisions/T1.md`.
- **Abstention / false-positive remediation** (`hermes/extraction/prompts.py`, `hermes/extraction/validator.py`, `hermes/schemas/examples/vehicle_fleet.py`, `tests/test_prompts.py`, `tests/test_validator.py`, `tests/test_schema_loader.py`, `tests/test_job_dedup.py`): **Prompts** now instruct the model to return **`[]`** when no row matches the schema (boilerplate / unrelated text) and to avoid placeholder or null-only rows; **`get_current_prompt_version()`** changes, so new **`contract_id`** values differ from pre-change runs (job dedup still keys on file hash + schema ref + pages + model, not prompt). **`parse_json_array`** treats empty dicts and all-**null** single objects as **`[]`** instead of coercing them to one spurious record; **`validate_with_repair`** drops validated rows whose fields are all **None** (user schemas with required nullable semantics should avoid all-optional models). **`VehicleRecord`** now requires **`numero_serie`** (VIN anchor); **`hermes init`** still copies examples only when missing—existing `~/.hermes/hermes_user/examples/vehicle_fleet.py` is unchanged until the user refreshes it. **CLI** (`hermes/cli.py`): verbose eval field-diff loop renamed **`field_diff`** to avoid shadowing **`fixture_dir`** (`fd`) and satisfy **mypy**.
- Clarified synthetic Excel fixture intent (stress/integration, historical filename) in `generate_test_datasets.py` and README; added **How we measure quality** (eval vs `hermes test`).
- **Eval runner** (`hermes/eval/runner.py`, `hermes/cli.py`, `tests/test_eval_runner.py`, `tests/test_eval_regression.py`): **`hermes eval`** runs manifests from **`--fixture-dir`** (default **`tests/fixtures/eval`**) or a single **`--manifest`**, obtains chunk results via **`run_pipeline`** (local LLM), **`--from-results`** (DB), or **`--from-jsonl`**, scores with **`score_fixture`**, prints a Rich summary table, optional **`--output`** JSON export, and **`--update-goldens`** with **`--yes`** or interactive confirm. **pytest** regression replays committed fixtures with a **mocked LLM** returning golden JSON so CI does not need an API key.
- **Eval frozen fixtures** (`tests/fixtures/eval/`, `tests/generate_fixtures.py`, `tests/test_eval_fixtures.py`, `.gitignore`): committed **`sample_excel`** and **`sample_pdf_text`** manifests plus **`.golden.jsonl`** baselines (VehicleRecord-shaped rows, second line **`[]`** per negative chunk) alongside copies of **`sample.xlsx`** / **`sample_text.pdf`**. **`sample.xlsx`** adds a **Boilerplate** sheet (chunk 1, negative); **`sample_text.pdf`** uses page 1 for slips (chunk 0) and page 2 for boilerplate (chunk 1). **`generate_fixtures.py`** syncs binaries into **`eval/`** after generation and embeds **`BOILERPLATE_EVAL_NEGATIVE`** in boilerplate for mocked eval tests. **`.gitignore`** allows **`tests/fixtures/eval/**`** while keeping other synthetic fixtures ignored. **Tests** load manifests, parse goldens, and assert scorer self-match on golden-vs-job replay.
- **Extraction** (`hermes/extraction/validator.py`, `hermes/extraction/pipeline.py`): **`validate_with_repair`** treats a parsed **`[]`** as success (no repair loop). **`_process_chunk`** persists **`extraction_results`** whenever validation succeeds, including **empty** record lists, so abstaining chunks appear in DB and eval scoring.
- **Eval scorer** (`hermes/eval/scorer.py`, `tests/test_eval_scorer.py`): **`score_fixture`** compares manifest chunk expectations to **`list[dict]` job results** (optional `record_json` / `records` / `validation_passed`) and optional per-chunk goldens; emits **`EvalResult`** with **`ChunkScore`**, **`FieldDiff`** (including **`error`** on golden parse failure), and **`EvalSummary`** (pass rates, negative false-positive rate, field accuracy when goldens exist). **`record_json`** is parsed like **`extraction_results`** / export (JSON array of objects). **`page_range`** expectations are flagged **`page_range_unresolved`** until the runner supplies **`chunk_index`**. Positive + validated output + no golden passes as **`schema_pass_no_golden`** (no field accuracy—known limitation). Logger **`hermes.eval.scorer`**. 
- **Eval value normalization** (`hermes/eval/normalize.py`, `tests/test_eval_normalize.py`): pure comparison helpers—string casefold/strip, `math.isclose`-style numeric equality, US-oriented currency and percent parsing, ISO/US date strings canonicalized with stdlib only—and **`normalize_value`** with optional type hints (`number`, `currency`, `percent`, `date`, `string`, `strict`) returning **`exact` / `normalized` / `mismatch` / `missing`** for downstream scoring. **Tests** cover whitespace/casefold, NaN handling, `$`-formatted amounts vs floats, percent strings vs fractions, cross-format dates, and strict vs lenient paths.
- **Eval manifests** (`pyproject.toml`, `hermes/eval/manifest.py`, `hermes/eval/__init__.py`, `tests/test_eval_manifest.py`): add **`pyyaml>=6.0`** as a core dependency; introduce **`ChunkLabel`**, **`PageRange`**, **`ChunkExpectation`**, and **`EvalManifest`** (Pydantic v2) with explicit manifest **`addressing`** (`chunk_index` / `page_range` / `mixed`) aligned per-chunk addressing; implement **`load_manifest(path)`** to parse and validate **`.manifest.yaml`** files. **Tests** cover valid loads, mixed addressing, YAML/root errors, union violations, addressing-mode mismatches, and empty chunk lists.
- **Lint / CI** (`hermes/eval/normalize.py`, `hermes/eval/runner.py`, `tests/generate_fixtures.py`, `tests/test_eval_fixtures.py`, `tests/test_eval_manifest.py`, `tests/test_eval_normalize.py`, `tests/test_eval_regression.py`, `tests/test_obs_schema.py`): address **Ruff** E501 (line length), I001 (import order), F401 (unused imports), and **UP042** (`ResultsMode` uses **`StrEnum`** instead of `(str, Enum)`) so **`ruff check .`** matches **`.github/workflows/ci.yml`**.

## 2026_04_13

- **CLI** (`hermes status`): job detail view includes a **Contract** row with the full **`contract_id`** (or `-` when unset). The **All Jobs** table adds a **Contract** column with a truncated id (28 characters max) so terminal tables stay readable.
- **Documentation** (`README.md`, `config.toml.example`): README documents Docker, job deduplication / **`--force`**, **`list-schemas`** flags, supported file extensions, **`hermes status`** contract column behavior, **`hermes test`** reuse vs **`--force`**, OCR timeout and large-Excel preflight notes, **`tiktoken_encoding`**, and **`-f`** semantics across commands. **`config.toml.example`** adds **`tiktoken_encoding`** under **`[extraction]`**.

## 2026_04_11

- **Extraction contracts** (`hermes/migrations/005_extraction_contracts.sql`, `hermes/extraction/contract_identity.py`, `hermes/db.py`, `hermes/models.py`, `hermes/extraction/pipeline.py`, `hermes/cli.py`): SQLite table **`extraction_contracts`** stores a **canonical JSON Schema** string (sorted keys, compact separators), **`json_schema_sha256`**, **`prompt_version`**, and **`schema_class`** (`module:Class` ref). **`contract_id`** is content-addressed (`ctr_` + 32 hex chars from a composite SHA-256 over canonical UTF-8 bytes, NUL, prompt version, NUL, schema ref). **`jobs`**, **`llm_runs`**, and **`extraction_results`** gain nullable **`contract_id`** FKs; existing rows stay NULL. Before the extraction phase (and on **`retry`** / resume when the job had no contract), Hermes **resolves or inserts** a contract, **updates the job**, and writes the same id on every run and result. Optional dedup reuses a row when hash + prompt + `schema_class` match. **Tests:** `tests/test_contract_identity.py`, `tests/test_db.py`, `tests/test_pipeline_integration.py`.
- **Dev workflow** (`Makefile`, `.gitignore`, `README.md`): **`tests/fixtures/`** is **gitignored** (synthetic output from `tests/generate_fixtures.py`). **`make test`** runs fixture generation then **`pytest`**; **`make ci`** matches **`.github/workflows/ci.yml`** (ruff, mypy, fixtures, pytest). System audit: **fixture workflow** closed for CI; **local** caveat documented (run **`make test`** or generate before **`pytest`**).
- **Deployment** (`Dockerfile`, `.dockerignore`): container image for the Hermes CLI on **Python 3.12** (`python:3.12-slim-bookworm`), `pip install` from package sources, non-root user **`hermes`** (uid 1000), `ENTRYPOINT` **`hermes`** with default **`--help`**. Optional build-arg **`PIP_EXTRAS`** (e.g. `[tiktoken]`, `[ocr]`) maps to `pip install ".${PIP_EXTRAS}"`. **Rationale:** closes audit gap for a reproducible deployment artifact; OCR/tiktoken remain opt-in via extras due to size and stack differences.
- **CLI** (`hermes list-schemas`, `hermes/schemas/discover.py`): prints sorted `module:Class` references for Pydantic models under bundled `hermes.schemas.examples` and `~/.hermes/hermes_user`, matching the rules used by `load_schema`. Failed user modules are skipped with a stderr warning. **`--no-packaged`** / **`--no-user`** narrow the scan. **`ensure_user_hermes_on_sys_path`** in `hermes/schemas/loader.py` is public (was `_ensure_user_hermes_on_sys_path`). **Tests:** `tests/test_discover.py`, `tests/test_cli.py` (`test_cli_list_schemas_packaged_only`). **Docs:** `README.md` Quick Start.
- **Documentation** (`README.md`): **Trust** for `--schema` and `default_schema`—Quick Start one-liner and **Custom Schemas** paragraph stating that `module.path:ClassName` is resolved by **importing** that module (same trust as running that import), that Hermes does **not** sandbox schema code, and that untrusted or unreviewed module paths should not be used. **Rationale:** closes system audit §6 (document trusted schema refs).
- **Job deduplication** (`hermes/migrations/004_job_dedup.sql`, `hermes/dedup.py`, `hermes/db.py`, `hermes/extraction/pipeline.py`): before preflight, compute **SHA-256** of the source file and the **effective LLM model** (`effective_llm_model` — same resolution as extraction: override, else LiteLLM model or Ollama model). If a **`completed`** job exists with the same `content_sha256`, `schema_class`, `pages_spec`, and `llm_model_key`, **`run_pipeline`** returns that job id and skips work (Rich message). New jobs persist `content_sha256` and `llm_model_key` on `jobs`. **`force_new_job`** on `run_pipeline` disables reuse. **CLI:** `hermes extract --force` / `-f` and `hermes test --force` / `-f` (without `--force`, `hermes test` may reuse a prior completed run for the same fixtures). **Caveat:** prompt version is **not** part of the dedup key; changing prompts alone can still hit reuse until `--force` or schema/pages/model/file bytes differ. **Tests:** `tests/test_job_dedup.py`.
- **Extraction-stage resume** (`hermes extract --resume <job_id>`, `hermes/extraction/pipeline.py`): after normalization and chunking are done, rebuilds chunks from stored normalized Markdown (`hermes/ingestion/storage.py` — `load_normalized_pages_from_store`), verifies `len(chunks) == jobs.total_chunks`, skips indices present in `extraction_results` (`get_successful_chunk_indices` in `hermes/db.py`), and continues the threaded/sequential extraction loop. **`jobs.pages_spec`** column (migration `003_pages_spec.sql`) records the CLI `--pages` string on new jobs. **Rationale:** process crash or Ctrl+C can leave a job partial; users should not re-OCR or pay again for finished chunks. Mid-normalization resume remains out of scope. **Docs:** `README.md` §8; quick reference. **Tests:** `tests/test_pipeline_integration.py` (`test_resume_pipeline_finishes_remaining_chunks`), `tests/test_storage.py`, `tests/test_db.py`.
- **Excel preflight token estimate** (`hermes/ingestion/preflight.py`): hybrid **sampling** for large workbooks—if summed per-sheet **`max_row`** exceeds **`EXCEL_PREFLIGHT_FULL_SCAN_MAX_ROWS`** (10_000), each sheet uses a **prefix** of up to **`EXCEL_PREFLIGHT_PREFIX_ROWS_PER_SHEET`** (500) rows and **extrapolates** total characters from dimensions; smaller workbooks keep a **full row scan** (exact). **`estimated_tokens`** uses **`CHARS_PER_TOKEN`** from **`hermes/normalization/chunker.py`** (same divisor as PDF preflight and chunking). **Rationale:** very large `.xlsx` files no longer iterate every row just for a rough CLI `~tokens` hint. **Tests** (`tests/test_preflight.py`): fixture matches full-scan reference; monkeypatched thresholds prove sampled path matches uniform content; empty + data sheet smoke test.
- **OCR timeout** (`hermes/normalization/pdf_ocr.py`, `hermes/config.py`): optional **`normalization.ocr_timeout_seconds`** (default **0** = no limit). When set, OCR runs in a worker thread with **`Future.result(timeout=...)`**; on timeout the page gets placeholder text and processing continues. **`shutdown(wait=False)`** avoids blocking the main thread on pool teardown. **Caveat:** this only ends the **wait** for the page result—it does **not** reliably cancel work inside Surya/EasyOCR; native OCR **may keep running** (CPU/GPU) until that call returns or the process exits. **Rationale:** pathological scans should not hang the CLI indefinitely; true cooperative cancellation in third-party OCR is out of scope.
- **Tests** (`tests/`): table-heavy **`chunk_pages`** coverage; **`LiteLLMClient`** unit tests with mocked `litellm.completion`; **`pdf_ocr`** tests with stubbed OCR and slow-OCR timeout; Typer **`CliRunner`** smoke tests for **`version`**, **`init`** (isolated home), **`export`**; pipeline test forcing two chunks with **`max_workers=2`** and asserting **`ThreadPoolExecutor(max_workers=2)`**. **`hermes test`** (full benchmark) remains out of default CI by design.
- **Chunking** (`hermes/normalization/chunker.py`): raised **`MAX_TABLE_ROWS_PER_CHUNK`** from **10** to **80** for oversized markdown tables. **Rationale:** smaller batches were tuned for weak local inference; larger row batches reduce LLM calls per sheet when using cloud models (e.g. gpt-4o-mini) while keeping each chunk’s JSON output bounded by the schema; tune further if validation or timeouts regress on very wide rows.

## 2026_04_10

- **PEP 561** (`hermes/py.typed`): empty marker so the package is treated as typed when installed. **Rationale:** without it, tools like mypy may not use `hermes`’s annotations in dependent projects (PEP 561).
- **`hermes init`** now installs editable example schema modules under `~/.hermes/hermes_user/examples/` (`vehicle_fleet.py`, `generic_table.py`), copied from the bundled examples only when each file is missing, so user edits are preserved on re-run.
- **New config from init** sets `default_schema` to `hermes_user.examples.generic_table:GenericRow` instead of the site-packages path, so extraction works from the user directory without touching installed packages.
- **Schema loading** prepends `~/.hermes` to `sys.path` before importing modules, so `hermes_user.examples.*` resolves without setting `PYTHONPATH`.
- **Documentation** (`README.md`) updates the Quick Start with the user-local schema paths and clarifies custom schema layout versus `~/.hermes`.
- **Tests** add coverage for user schema installation and loading (`tests/test_user_schemas.py`).
- **`hermes extract --pages`** (`hermes/cli.py`): optional page/sheet subset for large documents; help text documents 1-based PDF page numbers and Excel **sheet** indices (not rows); wired via `pages_spec` into `run_pipeline`.
- **Page spec parsing** (`hermes/ingestion/pages_spec.py`): supports ranges and lists (e.g. `1-10`, `3,5,7`); `resolve_page_indices_0` validates against preflight totals and yields 0-based indices for normalizers.
- **Pipeline** (`hermes/extraction/pipeline.py`): resolves `--pages` after preflight; passes `page_indices` to the router; preflight/normalization stage details note subset size; fails clearly if normalization returns no pages; chunking uses `normalized_pages`; `jobs.page_count` remains full document size.
- **Normalization** (`hermes/normalization/router.py`, `pdf_text.py`, `pdf_ocr.py`, `excel.py`): optional `page_indices` filters PDF iteration and Excel sheets; output still uses stable `page_{n}.md` / `sheet_{n}.md` names and original 0-based indices for `NormalizedPage` / chunk `source_pages` (DLQ chunk paths unchanged).
- **Normalization progress** (`hermes/extraction/pipeline.py`, router + normalizers): Rich progress bar for the normalize stage (same columns as extraction); normalizers accept optional `on_page_done(page_idx)` and invoke it after each sheet/page so the pipeline can advance the bar without coupling normalizers to Rich. **Rationale:** OCR/normalization often dominates wall time versus LLM extraction, which already had a bar; users need visible progress for long scans.
- **OCR model caching** (`hermes/normalization/pdf_ocr.py`): Surya detection/recognition models and the EasyOCR reader load once per process via `@lru_cache(maxsize=1)` helpers (`_get_surya_models`, `_get_easyocr_reader`) instead of on every page. **Rationale:** per-page reloads were a major performance bug on multi-page scans; keeping models resident for the process matches typical CLI usage (exit after the run).
- **Tests** (`tests/test_pages_spec.py`, `tests/test_normalization.py`): parser/validation coverage and PDF single-page filter smoke test.
- **Chunk token estimates** (`hermes/normalization/chunker.py`): optional `tiktoken` for tokenizer-accurate `estimate_tokens` and token-window splits when installed; falls back to the `CHARS_PER_TOKEN` heuristic if `tiktoken` is absent or `get_encoding` fails (e.g. unknown encoding name).
- **Token heuristic alignment** (`hermes/normalization/chunker.py`): `CHARS_PER_TOKEN` is **4**, matching preflight’s `total_chars // 4` (`hermes/ingestion/preflight.py`) and common English/Spanish character-per-token guidance. **Rationale:** one consistent estimate for chunk merge/split and cost preflight; the previous value (2) skewed chunking toward more, smaller segments than preflight implied. Hard caps (`MAX_TEXT_CHUNK_TOKENS`, `MAX_TABLE_CHUNK_TOKENS`) unchanged.
- **Config** (`hermes/config.py`): `extraction.tiktoken_encoding` (default `cl100k_base`) selects the tiktoken encoding when the optional dependency is present.
- **Packaging** (`pyproject.toml`): optional extra `tiktoken` (`tiktoken>=0.7`).
- **Documentation** (`README.md`): install note for the `tiktoken` extra and `extraction.tiktoken_encoding`.
- **Dependencies** (`pyproject.toml`): removed unused `tenacity`; the extraction validator never imported it. **Rationale:** `hermes/extraction/validator.py` already implements repair retries with a plain `while` loop and `max_retries` (from `cfg.llm.max_retries` via `hermes/extraction/pipeline.py`)—bounded attempts, no inter-attempt backoff. Keeping an extra dependency would not change behavior unless we added waits/backoff, which would risk rate limits and test timing drift.
- **Packaging** (`pyproject.toml`): `[tool.hatch.build.targets.wheel]` sets `packages = ["hermes"]` so the wheel includes SQL migrations under `hermes/migrations/`. **Rationale:** explicit wheel target avoids relying on implicit defaults and documents that migrations ship with the package.
- **Migrations layout:** SQL files moved from repo-root `migrations/` to `hermes/migrations/` so installed and editable layouts match the package tree.
- **Config** (`hermes/config.py`): `get_migrations_dir()` resolves to `Path(__file__).parent / "migrations"` instead of `parent.parent / "migrations"`. **Rationale:** the old path pointed at a non-package location that disappears after `pip install`, so `hermes init` could skip applying migrations silently.
- **Database connections** (`hermes/db.py`): added `open_db()` and `open_connection()` context managers (via `contextlib.contextmanager`) that wrap `init_db()` and `get_connection()` and always call `conn.close()` in `finally`. **Rationale:** matches manual open/close semantics but guarantees cleanup on exceptions and removes scattered `conn.close()` on every early-return path.
- **CLI** (`hermes/cli.py`): `test`, `status`, `retry`, `export`, and `init` use `with open_db()` (or a no-op body where only initialization matters) instead of bare `init_db()` + `close()`.
- **Pipeline** (`hermes/extraction/pipeline.py`): `run_pipeline()` holds the main job connection under `with open_db()`; `_process_chunk()` uses `with open_connection()` per chunk so parallel workers still get one connection each without re-running migrations every time. **Rationale:** same thread-safety as before (`get_connection` per chunk), with reliable teardown.
- **Database** (`hermes/db.py`, `hermes/extraction/pipeline.py`): `save_result`, `save_llm_run`, `save_pipeline_stage`, and `save_failed` accept `commit` (default `True`); `_process_chunk` passes `commit=False` and issues a single `conn.commit()` after all chunk writes. **Rationale:** per-call commits meant hundreds of WAL syncs per large job; one commit per chunk preserves chunk-level durability and matches DLQ retry semantics if a run dies mid-chunk.
- **Pipeline graceful shutdown** (`hermes/extraction/pipeline.py`): SIGINT sets a threading `Event`; the extraction loop checks it before each chunk (sequential) or before submitting further work (threaded). On interrupt, the extraction stage is recorded as interrupted, the job is set to **partial** if any chunk succeeded else **failed**, the DB connection closes via the existing `open_db` context, and a short progress summary is printed; threaded runs use `ThreadPoolExecutor.shutdown(wait=False, cancel_futures=True)`. **Rationale:** Ctrl+C should not leave the job stuck in `extracting` or require killing the process; incremental submit + cancel avoids queuing all chunk futures at once so shutdown can skip not-yet-started work.
- **Pipeline** (`hermes/extraction/pipeline.py`): added `_fail_job()` for early failure paths (unsupported file type, normalization exception, empty pages after normalization)—single place for saving the pipeline stage, setting `JobStatus.FAILED` with `normalization_error`, and printing the red error line. **Rationale:** removes three copies of the same sequence; behavior unchanged.
- **CI** (`.github/workflows/ci.yml`): GitHub Actions workflow on push to `main` and `dev` and on all pull requests—Python 3.12, editable install with `[dev]`, then `ruff check .`, `mypy hermes/`, `python tests/generate_fixtures.py`, and `pytest`. **Rationale:** fixtures are gitignored, so CI must generate them before tests; OCR-heavy tests remain skipped without optional `ocr` extras, matching the intended P2 scope; running on `dev` catches failures on the integration branch (including direct pushes) before merging to `main`.
- **Dev typing** (`pyproject.toml`): `types-openpyxl` added to `[dev]` for strict mypy against `openpyxl`; mypy overrides `ignore_missing_imports` for `surya.*`, `easyocr`, and `PIL.*` because those ship only with the optional `ocr` extra, not in default/CI dev installs.
- **Typing** (`hermes/config.py`): `import tomllib` only; removed the `tomli` fallback. **Rationale:** `requires-python >= 3.12` guarantees stdlib `tomllib`; the unused fallback imported a non-dependency and made mypy fail with `import-not-found` in CI (`# type: ignore[no-redef]` did not cover that error code).
- **Strict mypy pass** (`hermes/`): annotations and small API tweaks so `mypy hermes/` succeeds under `strict = true`—PyMuPDF calls annotated or narrowly suppressed (`open` is untyped in stubs), JSON parse paths in `validator.py` use `cast`, pipeline/CLI use `setattr` for dynamic LLM `model` and narrow `fail.id` before DLQ updates, `open_db`/`open_connection` generators typed, OCR helpers fully typed, `pdf_ocr` page iteration uses explicit `list[int]`. **Rationale:** keep CI’s type-check step meaningful without installing heavy optional OCR stacks on every run.
- **Lint** (`generate_test_datasets.py`, `hermes/cli.py`, `tests/`): ruff E501 addressed (file-level noqa for the long synthetic-data script; wrapped lines elsewhere) so `ruff check .` matches CI.
- **CLI** (`hermes/cli.py`): global `--verbose` / `-v` (Typer `@app.callback`) configures the root logger via `logging.basicConfig`—DEBUG when verbose, WARNING otherwise (`%(name)s %(levelname)s: %(message)s`). **Rationale:** pipeline, validator, LLM client, and PDF OCR use module loggers at INFO/DEBUG; with no `basicConfig`, everything below WARNING was discarded. Place the flag before the subcommand (e.g. `hermes -v extract …`).
- **Dead code** (`hermes/ingestion/storage.py`, `hermes/schemas/loader.py`): removed unused `read_raw()` and `discover_schemas()`—no callers in the tree. **Rationale:** shrinks the public-ish surface of those modules and avoids carrying dead helpers that suggest supported behavior.
- **Database** (`hermes/db.py`): `delete_job(conn, job_id)` removes rows from `extraction_results`, `llm_runs`, `failed_extractions`, and `pipeline_stages`, then deletes the `jobs` row (child tables first for FK order).
- **CLI** (`hermes/cli.py`): **`hermes clean`** removes a job’s storage directory (`{storage_base}/{job_id}/`) and calls `delete_job` for that id, or **`hermes clean --all`** for every job. **`typer.confirm()`** before destructive work; **`--force` / `-f`** skips confirmation for scripts. **Rationale:** gives a single, explicit way to reclaim disk and DB state without leaving orphan rows or files.
- **Export** (`hermes/db.py`, `hermes/cli.py`): `export_results_as_records` streams `record_json` from SQLite and yields parsed records (generator); `hermes export` writes JSONL or CSV row-by-row instead of building the full file in memory. CSV column order comes from the first record. **Rationale:** scales to large jobs without proportional RAM use for export.
- **CLI** (`hermes/cli.py`): **`hermes retry`** after the replay loop re-queries each affected job; promotion to **completed** (with `completed_chunks = total_chunks`, `failed_chunks = 0`) requires **no pending DLQ rows**, job status **partial** or **failed**, and **`COUNT(DISTINCT chunk_index)` in `extraction_results` equal to `jobs.total_chunks`** (via new `count_distinct_extraction_chunk_indices` in `hermes/db.py`). If the DLQ is empty but fewer chunks have results than `total_chunks` (e.g. graceful SIGINT left chunks never submitted—no DLQ row and no result), status is left unchanged and a short yellow message explains. **Rationale:** an empty DLQ does not imply every chunk was extracted; the previous check could mark the job complete and overstate progress.
- **LiteLLM client** (`hermes/extraction/llm_client.py`): `litellm.completion()` uses `num_retries=3` and `retry_strategy="exponential_backoff_retry"` so retriable failures (including 429) get spaced retries via LiteLLM’s `completion_with_retries` path. **Rationale:** `retry_after` is a `litellm.Router` setting and is not honored by `completion()` (it was effectively a no-op); `retry_strategy` is the supported knob for backoff on the completion API.
- **Config** (`hermes/config.py`): `_parse_config` uses `.get("litellm", {})` instead of `.pop()` and skips `litellm` when building `llm_fields`, so the loaded dict is not mutated and the raw nested table is not passed into `LLMConfig` (only the parsed `LiteLLMConfig` is). **Rationale:** callers/tests may reuse the same structure; mutation was easy to miss because `load_config` caches the result.
- **Typing** (`hermes/config.py`): `_parse_config(raw: dict[str, Any])` replaces a bare `dict` plus `# type: ignore[type-arg]`. **Rationale:** strict mypy expects explicit generic parameters on `dict`.
- **Typing** (`hermes/normalization/excel.py`): `_write_sheet_markdown` annotates the worksheet as `openpyxl.worksheet.worksheet.Worksheet` under `TYPE_CHECKING` so openpyxl is not imported at module import time. **Rationale:** removes `no-untyped-def` suppression while keeping the lazy `import openpyxl` inside `normalize_excel`.
- **Typing** (`hermes/normalization/pdf_ocr.py`): `_PdfPageLike` / `_PdfDocLike` / `_PixmapLike` `Protocol`s describe the PyMuPDF surface this module uses; `_ocr_page` and `_render_and_ocr` take `_PdfPageLike` instead of untyped parameters. **Rationale:** PyMuPDF exposes `Page` as a string forward declaration, so `pymupdf.Page` is not a valid mypy type; structural typing matches real usage. `pymupdf.open` keeps a narrow `# type: ignore[no-untyped-call]` because stubs still leave that entry point untyped under strict mypy.
- **Lint** (`hermes/models.py`): `FileType`, `JobStatus`, and `DLQStatus` now inherit from `enum.StrEnum` instead of `(str, Enum)`. **Rationale:** satisfies Ruff UP042 and matches Python 3.11+ guidance for string-valued enums.
- **Lint** (`hermes/extraction/pipeline.py`): wrapped long `_fail_job(...)` calls to stay within the 100-character line limit (Ruff E501).
